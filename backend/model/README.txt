# Place your trained model file here.
# Filename must be exactly: trained_model.keras
#
# This file is excluded from version control because it can be hundreds of MB.
# Add the following line to your .gitignore:
#   backend/model/*.keras
